import { AccessRootDirective } from './access-root.directive';

describe('AccessRootDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessRootDirective();
    expect(directive).toBeTruthy();
  });
});
