import { FirstUpDirective } from './first-up.directive';

describe('FirstUpDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstUpDirective();
    expect(directive).toBeTruthy();
  });
});
